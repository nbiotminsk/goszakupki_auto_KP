# Быстрый старт: Telegram интеграция

## Настройка Telegram бота

### 1. Создание бота через @BotFather

1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Придумайте имя бота (например, `Коммерческие предложения`)
   - Придумайте username бота (например, `kp_generator_bot`)
4. Сохраните полученный токен бота (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Получение Chat ID

Есть несколько способов получить Chat ID:

#### Способ 1: Через @userinfobot
1. Откройте @userinfobot в Telegram
2. Нажмите "Start"
3. Бот покажет ваш Chat ID

#### Способ 2: Создание группы
1. Создайте группу в Telegram
2. Добавьте вашего бота и любого другого человека (группу с одним ботом создать нельзя)
3. Отправьте любое сообщение в группу
4. Используйте API для получения Chat ID группы

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
```

Пример:

```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=-1001234567890
```

## Использование

### Автоматическая генерация и отправка

#### Через веб-интерфейс

1. Откройте приложение: `http://localhost:3001`
2. Вставьте URL закупки с портала goszakupki.by
3. Заполните необходимые поля (цены, единицы измерения и т.д.)
4. Нажмите "Сгенерировать"
5. После успешной генерации нажмите "📤 Отправить в Telegram"
6. Файл будет отправлен в указанный чат с сообщением, содержащим:
   - 📢 Краткое название организации (из API налоговой службы)
   - ⏰ Подать до (с парсинга страницы)
   - 🔗 Ссылка на закупку

#### Через API

Пример запроса для генерации и отправки:

```bash
curl -X POST http://localhost:3001/generate \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://goszakupki.by/single-source/view/123456",
    "unitPrice": "1500",
    "unitPrice2": "0",
    "includeLot1": true,
    "includeLot2": false,
    "freeDescription": "Доставка включена",
    "unit1": "шт.",
    "unit2": ""
  }'
```

Пример ответа:

```json
{
  "success": true,
  "fileName": "2024_01_15_10_30_45_123456.pdf",
  "filePath": "/path/to/file.pdf",
  "url": "https://goszakupki.by/single-source/view/123456",
  "companyShortName": "ООО \"Техно-Сервис\"",
  "proposalEndDate": "31.01.2024 18:00"
}
```

Затем отправка в Telegram:

```bash
curl -X POST http://localhost:3001/send-to-telegram \
  -H "Content-Type: application/json" \
  -d '{
    "chatId": "-1001234567890",
    "fileName": "2024_01_15_10_30_45_123456.pdf",
    "url": "https://goszakupki.by/single-source/view/123456",
    "caption": "Коммерческое предложение",
    "companyShortName": "ООО \"Техно-Сервис\"",
    "proposalEndDate": "31.01.2024 18:00"
  }'
```

### Ручная генерация

#### Через веб-интерфейс

1. Откройте страницу ручного ввода: `http://localhost:3001/manual.html`
2. Заполните все обязательные поля:
   - Название организации
   - УНП (опционально, для автозаполнения)
   - Адрес
   - Данные о лотах
   - Срок поставки (используется как дата окончания)
3. Нажмите "Сгенерировать PDF"
4. После успешной генерации нажмите "📤 Отправить в Telegram"

#### Через API

```bash
curl -X POST http://localhost:3001/api/generate-manual \
  -H "Content-Type: application/json" \
  -d '{
    "customerName": "ГУ \"Минский городской дворец спорта\"",
    "customerUnp": "100123456",
    "customerAddress": "г. Минск, пр. Победителей, 108",
    "includeLot1": true,
    "includeLot2": false,
    "lot1Items": [
      {
        "name": "Оборудование",
        "quantity": "10",
        "price": "1500"
      }
    ],
    "place": "г. Минск",
    "payment": "Безналичный расчет",
    "endDate": "в течение 5 дней с момента оплаты",
    "freeDescription": "Гарантия 12 месяцев"
  }'
```

## Формат сообщений в Telegram

### Сообщение с полными данными

```
📋 Коммерческое предложение
📢 Организация: ГУ "Минский городской дворец спорта"
⏰ Подать до: 31.01.2024 18:00

🔗 [Ссылка на закупку](https://goszakupki.by/single-source/view/123456)
```

### Сообщение без даты окончания (если не найдена)

```
📋 Коммерческое предложение
📢 Организация: ООО "Техно-Сервис"

🔗 [Ссылка на закупку](https://goszakupki.by/single-source/view/123456)
```

### Сообщение только со ссылкой (минимальный вариант)

```
🔗 [Ссылка на закупку](https://goszakupki.by/single-source/view/123456)
```

## Проверка работы

### Проверка статуса бота

```bash
curl http://localhost:3001/telegram-status
```

Ответ:

```json
{
  "available": true,
  "botInfo": {
    "success": true,
    "id": 123456789,
    "username": "kp_generator_bot",
    "first_name": "Коммерческие предложения"
  },
  "message": "Telegram бот доступен"
}
```

### Тестирование с помощью скрипта

```bash
node test/telegram_test.js
```

Скрипт автоматически проверит:
- ✅ Доступность Telegram бота
- ✅ Получение информации о боте
- ✅ Доступность чата
- ✅ Отправку сообщения с кратким названием организации
- ✅ Отправку сообщения с датой окончания
- ✅ Отправку сообщения с обоими параметрами

## Устранение неполадок

### Бот не инициализирован

**Проблема:** В консоли: `⚠️ Токен Telegram бота не найден`

**Решение:** Убедитесь, что в файле `.env` задана переменная `TELEGRAM_BOT_TOKEN`

### Chat ID не найден

**Проблема:** `Необходимо указать Chat ID или настроить TELEGRAM_CHAT_ID в .env`

**Решение:** Добавьте в `.env` переменную `TELEGRAM_CHAT_ID` или укажите Chat ID при отправке

### Бот заблокирован

**Проблема:** `Бот был заблокирован пользователем`

**Решение:** 
1. Найдите бота в Telegram
2. Отмените блокировку
3. Нажмите "Start" в диалоге с ботом

### Чат не найден

**Проблема:** `Чат не найден. Проверьте правильность Chat ID`

**Решение:** 
- Для личных сообщений: проверьте Chat ID через @userinfobot
- Для групп: убедитесь, что бот добавлен в группу и имеет права на отправку сообщений

### Ошибка SSL/TLS

**Проблема:** Ошибка при запросе к API налоговой службы

**Решение:** Проверьте подключение к интернету. Если проблема persists, убедитесь, что в коде отключена проверка SSL сертификата (уже реализовано в parser.js)

## Дополнительные возможности

### Автозаполнение данных организации

При указании УНП система автоматически получает данные из API налоговой службы:
- Полное название организации
- Краткое название организации (используется в Telegram)
- Юридический адрес
- Дату регистрации
- Статус организации

Пример запроса:

```bash
curl -X POST http://localhost:3001/api/company-data \
  -H "Content-Type: application/json" \
  -d '{"unp": "100123456"}'
```

Ответ:

```json
{
  "success": true,
  "data": {
    "unp": "100123456",
    "fullName": "Государственное учреждение \"Минский городской дворец спорта\"",
    "shortName": "ГУ \"Минский городской дворец спорта\"",
    "address": "г. Минск, пр. Победителей, 108",
    "registrationDate": "01.01.2000",
    "taxOfficeCode": "123",
    "taxOfficeName": "Инспекция МНС по г. Минску",
    "statusCode": "1",
    "statusName": "Действующий",
    "statusChangeDate": null
  }
}
```

### Кеширование данных

Система автоматически кеширует результаты парсинга страниц для ускорения повторных запросов. Кеш очищается при каждом новом запросе к другому URL.

## Безопасность

### Защита токена

Никогда не коммитьте файл `.env` в Git. Добавьте его в `.gitignore`:

```
.env
```

### Ограничение доступа

На продакшене рекомендуется:
1. Использовать HTTPS для всех API запросов
2. Добавить аутентификацию для API endpoints
3. Ограничить частоту запросов (rate limiting)
4. Валидировать все входные данные

## Полезные ссылки

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [BotFather](https://t.me/botfather)
- [API налоговой службы РБ](https://grp.nalog.gov.by/api/grp-public/)
- [Документация Puppeteer](https://pptr.dev/)