# 🚀 Деплой на Railway

Это руководство поможет вам развернуть проект генератора коммерческих предложений на хостинге Railway.

## 📋 Требования

- Аккаунт на GitHub (уже должен быть, т.к. репозиторий там)
- Аккаунт на Railway (можно бесплатно зарегистрироваться через GitHub)
- Telegram бот с токеном (если планируете использовать отправку в Telegram)

## 🔧 Предварительная подготовка

### 1. Убедитесь, что все изменения запушены в GitHub

```bash
git status
git add .
git commit -m "Подготовка к деплою на Railway"
git push
```

### 2. Проверьте наличие файла `railway.toml`

Файл уже создан в проекте и содержит настройки для установки системных пакетов, необходимых для работы Puppeteer.

## 📝 Пошаговая инструкция деплоя

### Шаг 1: Создание аккаунта на Railway

1. Перейдите на [railway.app](https://railway.app/)
2. Нажмите кнопку "Sign Up" (или "Start New Project")
3. Авторизуйтесь через свой GitHub аккаунт
4. После входа вы попадёте в dashboard Railway

### Шаг 2: Создание нового проекта

1. Нажмите кнопку **"+ New Project"** вверху слева
2. Выберите опцию **"Deploy from GitHub repo"**
3. Railway запросит доступ к вашему GitHub (разрешите)
4. В списке репозиториев найдите и выберите **`nbiotminsk/goszakupki_auto_KP`**
5. Нажмите кнопку **"Add Variables"** (или сразу "Deploy" если хотите добавить переменные позже)

### Шаг 3: Настройка переменных окружения

Перед деплоем или после него нужно добавить переменные окружения:

#### Обязательные переменные:

**Для Telegram бота (если используете):**
- `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота
- `TELEGRAM_CHAT_ID` - ID чата, куда отправлять файлы (опционально, можно указывать в запросе)

#### Как добавить переменные:

1. В проекте Railway перейдите на вкладку **"Variables"** (или "Settings" → "Variables")
2. Нажмите кнопку **"+ New Variable"**
3. Введите имя переменной (например, `TELEGRAM_BOT_TOKEN`)
4. Введите значение (ваш токен)
5. Нажмите **"Add"**
6. Повторите для всех необходимых переменных

### Шаг 4: Деплой проекта

1. После добавления переменных, нажмите кнопку **"Deploy"**
2. Railway начнёт процесс деплоя:
   - Загрузка кода из GitHub
   - Установка npm зависимостей
   - Установка системных пакетов (APT packages из railway.toml)
   - Сборка проекта
   - Запуск приложения
   
3. Процесс обычно занимает 2-5 минут

### Шаг 5: Мониторинг деплоя

1. На вкладке **"Deployments"** вы увидите прогресс деплоя
2. Нажмите на активный деплой, чтобы увидеть логи
3. Если возникнут ошибки, они будут отображены в логах

### Шаг 6: Получение URL приложения

После успешного деплоя:

1. Railway автоматически назначит URL вашему приложению (например: `https://your-app-name.railway.app`)
2. URL можно найти:
   - На главной странице проекта в поле **"Public Domain"**
   - Или на вкладке **"Settings"** → "Networking"
3. Теперь ваше приложение доступно по этому URL!

## 🔍 Проверка работы приложения

### 1. Проверка работоспособности

Откройте в браузере: `https://your-app-name.railway.app/health`

Должен вернуться JSON ответ:
```json
{
  "status": "OK",
  "timestamp": "2024-01-01T12:00:00.000Z",
  "uptime": 123.456
}
```

### 2. Проверка Telegram бота (если настроен)

Откройте: `https://your-app-name.railway.app/telegram-status`

Должен вернуться JSON ответ:
```json
{
  "available": true,
  "botInfo": {
    "id": 123456789,
    "username": "your_bot_name",
    "first_name": "Your Bot"
  },
  "message": "Telegram бот доступен"
}
```

### 3. Тестирование генерации PDF

1. Перейдите на главную страницу: `https://your-app-name.railway.app/`
2. Введите URL закупки с портала goszakupki.by
3. Укажите цены за единицу
4. Нажмите кнопку генерации
5. Проверьте, что PDF файл успешно создаётся и скачивается

## 🛠️ Обновление проекта

Когда вы внесёте изменения в код и запушите их в GitHub:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Railway автоматически:
1. Обнаружит новый коммит
2. Создаст новый деплой
3. Обновит приложение

**Важно:** Старые версии будут доступны в истории деплоев.

## 📊 Мониторинг логов

### Просмотр логов в реальном времени:

1. Перейдите на вкладку **"Deployments"**
2. Нажмите на активный деплой
3. Нажмите кнопку **"Logs"** (или "View Logs")
4. Вы увидите логи приложения в реальном времени

### Поиск ошибок:

Если приложение не работает:
1. Проверьте логи на наличие ошибок (красный текст)
2. Проверьте переменные окружения
3. Убедитесь, что Node.js версия соответствует требованиям (>=18.0.0)

## 💰 Информация о тарифах Railway

**Бесплатный план (Free Tier):**
- $5 кредитов в месяц
- Около 512 часов работы (примерно 21 день不间断)
- 512 MB RAM
- Подходит для тестирования и небольшого использования

**Платные планы:**
- При необходимости большего количества ресурсов можно перейти на платный план
- Стоимость от $5/мес

## ⚠️ Возможные проблемы и решения

### Проблема: Puppeteer не запускается

**Решение:** 
- Проверьте, что файл `railway.toml` содержит правильные APT пакеты
- Проверьте логи деплоя на ошибки установки пакетов

### Проблема: Приложение падает с ошибкой Out of Memory

**Решение:**
- Увеличьте выделение памяти в настройках Railway:
  - Перейдите в "Settings" → "Configuration"
  - Измените "Memory" (например, на 1024MB)
  - Это потребует платный план

### Проблема: Telegram бот недоступен

**Решение:**
- Проверьте переменную `TELEGRAM_BOT_TOKEN`
- Убедитесь, что токен правильный и бот создан через BotFather
- Проверьте `/telegram-status` endpoint

### Проблема: Не генерируется PDF

**Решение:**
- Проверьте логи на наличие ошибок
- Убедитесь, что URL закупки правильный и доступен
- Проверьте, что Puppeteer успешно запускается

## 🔄 Альтернатива: Командная строка Railway CLI

Для опытных пользователей можно использовать Railway CLI:

```bash
# Установка Railway CLI
npm install -g @railway/cli

# Логин
railway login

# Создание проекта и деплой
railway init
railway up

# Добавление переменных окружения
railway variables set TELEGRAM_BOT_TOKEN=ваш_токен
railway variables set TELEGRAM_CHAT_ID=ваш_chat_id

# Просмотр логов
railway logs

# Открытие приложения в браузере
railway open
```

## 📞 Дополнительная информация

- [Официальная документация Railway](https://docs.railway.app/)
- [Railway Node.js Guide](https://docs.railway.app/guides/nodejs)
- [Railway Pricing](https://railway.app/pricing)

## ✅ Проверочный список перед деплоем

- [ ] Все изменения запушены в GitHub
- [ ] Файл `railway.toml` создан и находится в корне проекта
- [ ] Создан аккаунт на Railway
- [ ] Railway имеет доступ к GitHub репозиторию
- [ ] Настроены все необходимые переменные окружения
- [ ] Протестировано локально с помощью `npm start`
- [ ] Токен Telegram бота получен (если используется)

## 🎉 Поздравляю!

После успешного деплоя ваше приложение генерации коммерческих предложений будет доступно 24/7 по URL вида `https://your-app-name.railway.app`!

Теперь вы можете:
- Генерировать коммерческие предложения онлайн
- Отправлять результаты в Telegram
- Делиться ссылкой на приложение с коллегами

Если возникнут вопросы или проблемы, не стесняйтесь обращаться в техническую поддержку Railway или в репозиторий проекта.