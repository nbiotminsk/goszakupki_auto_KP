# План улучшений проекта Goszakupki Auto KP

Основываясь на анализе текущей архитектуры и кодовой базы, ниже представлен план по улучшению проекта, разделенный по приоритетам и категориям.

## 1. Тестирование и Качество Кода (Приоритет №1) 🛠
Текущее состояние: Используются разрозненные скрипты в папке `test/` для ручного запуска проверок.

*   [ ] **Внедрить Test Runner (Jest):**
    *   Перенести существующие скрипты (`test-all-three.js` и др.) на фреймворк Jest.
    *   Настроить команду `npm test` для запуска всех тестов.
    *   Добавить отчеты о покрытии кода (code coverage).
*   [ ] **Валидация входных данных:**
    *   Внедрить библиотеку **Joi** или **Zod** в `server.js`.
    *   Валидировать URL (формат, домен), цены (неотрицательные числа) и другие параметры API перед обработкой.
*   [ ] **Линтинг и форматирование:**
    *   Настроить **ESLint** для поиска потенциальных ошибок.
    *   Настроить **Prettier** для автоматического форматирования кода.

## 2. Архитектура и Надежность 🏗
Текущее состояние: Прямой вызов Puppeteer на каждый запрос, жестко закодированные селекторы.

*   [ ] **Очередь задач (Queue):**
    *   Внедрить очередь (например, `p-queue` для in-memory или `bull` для Redis), чтобы ограничить количество одновременных инстансов Chrome.
    *   Это предотвратит перегрузку сервера при множественных запросах.
*   [x] **Конфигурация Селекторов:**
    *   Вынести CSS-селекторы из `parser.js` в отдельный JSON-конфиг (`selectors.json`).
    *   Это позволит быстро адаптироваться к изменениям верстки сайта-источника без правки кода.
*   [ ] **Устойчивость парсера:**
    *   Добавить механизм **Retry** (повторные попытки) при сетевых ошибках или таймаутах Puppeteer.

## 3. Frontend и UX (Пользовательский опыт) 🎨
Текущее состояние: Базовая форма, отсутствие обратной связи о деталях процесса.

*   [ ] **Индикация прогресса:**
    *   Реализовать отображение стадий процесса на клиенте: "Парсинг...", "Генерация...", "Отправка...".
*   [ ] **Предпросмотр данных:**
    *   Добавить шаг подтверждения: Парсинг -> Отображение данных в форме -> Редактирование пользователем -> Генерация PDF.
*   [ ] **История генераций:**
    *   Реализовать простое хранилище (например, `lowdb` или `SQLite`) для истории последних сгенерированных КП.

## 4. DevOps и Деплой 🚀
Текущее состояние: Есть Dockerfile, нет оркестрации.

*   [ ] **Docker Compose:**
    *   Создать `docker-compose.yml` для упрощения запуска (приложение + потенциальная БД/Redis).
*   [ ] **Автоочистка:**
    *   Настроить автоматическое удаление старых PDF файлов из папки `generated/` (при старте).

## 5. Telegram Бот 🤖
Текущее состояние: Работает только на отправку уведомлений.

*   [ ] **Двусторонняя коммуникация:**
    *   Реализовать прием ссылок ботом.
    *   Сценарий: Пользователь кидает ссылку в чат -> Бот возвращает готовый PDF файл.
