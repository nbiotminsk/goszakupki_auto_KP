# Настройка Telegram бота для отправки документов

Это руководство поможет вам настроить автоматическую отправку PDF файлов и ссылок на закупки в Telegram.

## Содержание
1. [Создание Telegram бота](#создание-telegram-бота)
2. [Получение токена бота](#получение-токена-бота)
3. [Получение Chat ID](#получение-chat-id)
4. [Настройка переменных окружения](#настройка-переменных-окружения)
5. [Использование](#использование)
6. [Устранение проблем](#устранение-проблем)

---

## Создание Telegram бота

### Шаг 1: Создайте бота через BotFather

1. Откройте Telegram и найдите бота **@BotFather**
2. Нажмите **Запустить (Start)**
3. Отправьте команду `/newbot`
4. Введите имя бота (например: `My KP Sender`)
5. Введите username бота (например: `my_kp_sender_bot` - должен заканчиваться на `bot`)
6. Поздравляем! Вы создали бота.

### Шаг 2: Получите токен бота

BotFather пришлет вам сообщение с токеном в формате:
```
Use this token to access the HTTP API:
123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

**Важно:** Сохраните этот токен, он понадобится для настройки!

---

## Получение Chat ID

### Для личного чата

1. Откройте Telegram и найдите бота **@userinfobot**
2. Нажмите **Запустить (Start)**
3. Бот отправит вам сообщение с вашим Chat ID:
   ```
   Id: 123456789
   ...
   ```

### Для группы/канала

1. Добавьте вашего бота в группу или канал (созданного на шаге 1)
2. Сделайте бота администратором группы/канала
3. Отправьте любое сообщение в группу/канал
4. В браузере перейдите по ссылке:
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
   Замените `<ВАШ_ТОКЕН>` на токен вашего бота
5. В ответе найдите объект `chat` и скопируйте значение `id` (для групп оно будет отрицательным, например: `-100123456789`)

---

## Настройка переменных окружения

### Вариант 1: Настройка через командную строку (Windows PowerShell)

```powershell
# Установите токен бота
$env:TELEGRAM_BOT_TOKEN = "123456789:ABCdefGHIjklMNOpqrsTUVwxyz"

# Запустите сервер
npm start
```

### Вариант 2: Создание файла .env

1. Создайте файл `.env` в корне проекта (рядом с `package.json`)
2. Добавьте в него следующую строку:
   ```
   TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
   ```
3. Установите пакет dotenv (если еще не установлен):
   ```bash
   npm install dotenv
   ```
4. Обновите начало файла `server.js`:
   ```javascript
   require('dotenv').config();
   const express = require("express");
   // ... остальной код
   ```

### Вариант 3: Настройка системной переменной окружения

**Windows (CMD):**
```cmd
setx TELEGRAM_BOT_TOKEN "123456789:ABCdefGHIjklMNOpqrsTUVwxyz"
```

**Windows (PowerShell):**
```powershell
[System.Environment]::SetEnvironmentVariable('TELEGRAM_BOT_TOKEN', '123456789:ABCdefGHIjklMNOpqrsTUVwxyz', 'User')
```

После настройки перезапустите терминал/IDE для применения изменений.

---

## Использование

### Проверка работы бота

1. Запустите сервер:
   ```bash
   npm start
   ```

2. Откройте в браузере:
   ```
   http://localhost:3001/telegram-status
   ```

   Если все настроено правильно, вы увидите:
   ```json
   {
     "available": true,
     "botInfo": {
       "success": true,
       "id": 123456789,
       "username": "my_kp_sender_bot",
       "first_name": "My KP Sender"
     },
     "message": "Telegram бот доступен"
   }
   ```

### Отправка документа через веб-интерфейс

1. Откройте генератор: `http://localhost:3001`
2. Заполните форму для генерации коммерческого предложения
3. В поле **"Telegram Chat ID"** введите ваш Chat ID (полученный ранее)
4. Нажмите **"Сгенерировать коммерческое предложение"**
5. После успешной генерации нажмите кнопку **"📤 Отправить в Telegram"**
6. Проверьте ваш Telegram - документ должен быть доставлен!

### Отправка через API

Вы также можете отправить документ программно, отправив POST запрос на `/send-to-telegram`:

```bash
curl -X POST http://localhost:3001/send-to-telegram \
  -H "Content-Type: application/json" \
  -d '{
    "chatId": "123456789",
    "fileName": "2024_01_15_14_30_45_2886798.pdf",
    "url": "https://goszakupki.by/single-source/view/2886798",
    "caption": "Коммерческое предложение"
  }'
```

---

## Устранение проблем

### Проблема: "Telegram бот недоступен"

**Причины:**
- Не настроен токен бота (TELEGRAM_BOT_TOKEN)
- Неверный токен бота
- Бот был удален или деактивирован

**Решение:**
1. Проверьте, что переменная окружения `TELEGRAM_BOT_TOKEN` установлена
2. Проверьте правильность токена через BotFather
3. Перезапустите сервер

### Проблема: "Чат не найден"

**Причины:**
- Неверный Chat ID
- Бот не добавлен в группу/канал
- Вы не запустили бота через `/start`

**Решение:**
1. Проверьте правильность Chat ID через @userinfobot
2. Для групп/каналов: добавьте бота и сделайте его администратором
3. Для личных чатов: откройте диалог с ботом и нажмите "Start"

### Проблема: "Бот был заблокирован пользователем"

**Причины:**
- Пользователь заблокировал бота

**Решение:**
1. Пользователь должен разблокировать бота в настройках Telegram
2. Отправить боту команду `/start`

### Проблема: Файл не отправляется

**Причины:**
- Файл слишком большой (лимит Telegram: 50 МБ)
- Файл не существует на сервере

**Решение:**
1. Проверьте размер файла в папке `generated`
2. Убедитесь, что файл существует по указанному пути
3. Проверьте логи сервера на наличие ошибок

---

## Безопасность

⚠️ **Важно:** Никогда не публикуйте ваш токен бота в публичных репозиториях!

1. Добавьте файл `.env` в `.gitignore`:
   ```
   .env
   node_modules/
   generated/
   ```

2. Не передавайте токен третьим лицам

3. Регулярно меняйте токен бота через BotFather командой `/revoke`

---

## Дополнительные возможности

### Добавление подписи к сообщению

Вы можете добавить текстовое сообщение, которое будет отправлено вместе с PDF файлом:

```javascript
await telegramSender.sendPDFWithLink(
  chatId,
  filePath,
  fileName,
  url,
  "Коммерческое предложение по лоту №1" // Подпись
);
```

### Отправка только ссылки

Если вам нужно отправить только ссылку без файла:

```javascript
await telegramSender.sendLink(
  chatId,
  url,
  "Проверьте эту закупку"
);
```

---

## Контакты и поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте логи сервера в консоли
2. Убедитесь, что все зависимости установлены: `npm install`
3. Проверьте версию Node.js: `node --version` (требуется >= 18.0.0)

---

## Полезные ссылки

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [BotFather](https://t.me/botfather)
- [UserInfoBot](https://t.me/userinfobot)
- [Документация node-telegram-bot-api](https://github.com/yagop/node-telegram-bot-api)