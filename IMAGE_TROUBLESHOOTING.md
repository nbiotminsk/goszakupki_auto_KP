# Устранение проблем с изображениями в PDF

## 🖼️ Проблема: Изображения не отображаются в сгенерированном PDF

### ✅ Что уже сделано для исправления:

1. **Исправлены пути в HTML-шаблоне:**
   - Заменены относительные пути `../images/logo.png` на плейсхолдеры `{{LOGO_PATH}}`
   - Заменены относительные пути `../images/pechat.jpg` на плейсхолдеры `{{PECHAT_PATH}}`

2. **Обновлен генератор PDF:**
   - Добавлена подстановка абсолютных путей к файлам
   - Добавлены флаги безопасности для Puppeteer
   - Добавлено время ожидания загрузки изображений

3. **Добавлена проверка существования файлов**

### 🔧 Если изображения все еще не отображаются:

#### 1. Проверьте наличие файлов:
```bash
ls -la images/
```
Должны быть файлы:
- `logo.png` (83.7 KB)
- `pechat.jpg` (613 KB)

#### 2. Проверьте права доступа:
```bash
# Проверить права на файлы
ls -l images/logo.png images/pechat.jpg

# Установить права если нужно
chmod 644 images/*.png images/*.jpg
```

#### 3. Запустите тест:
```bash
node test-images.js
```

#### 4. Проверьте сгенерированный файл:
- Откройте PDF из папки `generated/`
- Проверьте наличие логотипа в шапке документа
- Проверьте наличие печати в подписи директора

### 🛠️ Альтернативные решения:

#### Вариант 1: Встроить изображения в HTML как base64
Если проблема остается, можно преобразовать изображения в base64 и встроить прямо в HTML:

```javascript
// Добавить в pdfGenerator.js
const fs = require('fs');

function imageToBase64(imagePath) {
    const image = fs.readFileSync(imagePath);
    return `data:image/${path.extname(imagePath).slice(1)};base64,${image.toString('base64')}`;
}

// Использовать в replaceTemplateVariables:
const logoBase64 = imageToBase64(path.join(__dirname, 'images', 'logo.png'));
result = result.replace(/{{LOGO_PATH}}/g, logoBase64);
```

#### Вариант 2: Использовать URL вместо локальных файлов
Если генерация происходит на веб-сервере:

```javascript
result = result.replace(/{{LOGO_PATH}}/g, 'http://localhost:3000/images/logo.png');
result = result.replace(/{{PECHAT_PATH}}/g, 'http://localhost:3000/images/pechat.jpg');
```

### 🐛 Распространенные проблемы:

1. **Путь содержит пробелы или кириллицу**
   - Решение: Используйте пути без пробелов и спецсимволов

2. **Файлы изображений повреждены**
   - Решение: Проверьте, что файлы открываются в просмотрщиках изображений

3. **Недостаточно прав доступа**
   - Решение: Запустите от имени администратора или измените права

4. **Блокировка безопасности браузера**
   - Решение: Флаги `--allow-file-access-from-files` и `--disable-web-security` уже добавлены

### 📞 Если ничего не помогает:

1. Проверьте версию Node.js и Puppeteer
2. Попробуйте обновить зависимости: `npm update`
3. Создайте issue с подробным логом ошибки

### ✅ Проверка работоспособности:

После исправлений запустите полную систему:
```bash
npm start
```

И протестируйте генерацию PDF через веб-интерфейс по адресу:
http://localhost:3000